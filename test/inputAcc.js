// inputAcc.js
function do_something(x) {
    // This is a sample comment
    x = 5 /*
    Halo bang
    */ y = "Haig\'ais"
    x = 'Hai\"bang' // Ini contoh esc
    y = "Haig\'ais"
    for(x,y;y;z){
      true
    }
    if (v) {
      return 0 
    } else if (x + 4 == 1) {
      if (true) {
        return 3.5
      } else {
        y = "ak\"u"
        return 2
      }
    } else if (x == 32) {
      return 4
    } else {
      return "Momen"
    }
  }