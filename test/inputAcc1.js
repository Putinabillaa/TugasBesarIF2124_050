// inputAcc.js
function do_something(x) {
    // This is a sample comment
    /*
    Halo bang
    */
    let x = 1 + 2
    var x
    const v = 2
    x = '1' + '2'
    y = {x: no}
    for(y;y;z){
      true
    }
    if (v) {
      return 0 
    } else if (x + 4 == 1) 
    {
      if (true) {
        return 3.5
      } else {
        y = "ak\"u"
        return 2
      }
    } 

    else if (x == 32) {
      return 4
    } 
    else {return "Momen\'"}
  }